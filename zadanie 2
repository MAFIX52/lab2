#include<iostream>
using namespace std;
int main()
{
	setlocale(0, "");
	int num = 0, t = 0, d = 1;
	cout << "Ведите число: ";
	cin >> num;
	while (num)
	{
		num = num / 2;
		d = d * 10; 
		t += (num % 2) * d;
	}
	cout << t;

	
	return 0;
}
